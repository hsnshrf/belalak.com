{
  "name": "voicevault",
  "private": true,
  "version": "0.1.0",
  "description": "VoiceVault — cross-platform recording, transcription & search",
  "workspaces": [
    "packages/shared",
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "build:shared": "npm run build --workspace packages/shared",
    "typecheck": "npm run build:shared && npm run typecheck --workspaces --if-present",
    "lint": "npm run build:shared && npm run lint --workspaces --if-present",
    "test": "npm run build:shared && npm run test --workspaces --if-present",
    "check": "npm run build:shared && npm run lint --workspaces --if-present && npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=20"
  }
}
